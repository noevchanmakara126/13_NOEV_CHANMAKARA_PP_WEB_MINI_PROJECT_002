import headerToken from "@/lib/headerToken";
import { getWorkspaces } from "./workspace/workSpaceService";

export const getTaskByWorkspaceId = async () => {
  //   const workspace = await getWorkspaces();
  //   console.log("this is workspace ", workspace?.payload);
  //   const headers = await headerToken();
  //   console.log("this is header ", headers);
  //   const tasks = await fetch(
  //     `${process.env.NEXT_PUBLIC_AUTH_BASE_URL}/tasks/workspace=${workspace}`,
  //     {
  //       Authorization: `Bearer ${session?.user?.token}`,
  //     }
  //   );
  //   const res = await tasks.json();

  return null;
};
